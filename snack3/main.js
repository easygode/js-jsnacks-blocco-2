const nomi = ['Paolo', 'Silvio', 'Anna', 'Costanza', 'Hotel', 'Telefon', 'Golf'];
const cognomi  = ['Giullare', 'Kiloz', 'Giuitj', 'Fernemi', 'Giuop', 'Firnemr'];
const invitati = [];

for (let i = 0; i < 3; i++) {
    randomNomi = Math.floor(Math.random() * nomi.length);
    randomCognomi = Math.floor(Math.random() * cognomi.length);
    
    const invitati = `$randomNomi`
}